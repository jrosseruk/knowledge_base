# References

All citations across the knowledge base resolve into this page. Each entry is
back-linked from every page that cites it — click the ↩ arrows next to the key
to jump back.

Bibliography source: `references.bib` in the repo root.

```{bibliography}
:all:
```
