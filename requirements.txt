# Pin to classic (Sphinx-based) Jupyter Book.
# Jupyter Book 2.x is a separate, MyST-engine-based tool that requires Node.js
# and uses a completely different config format.
jupyter-book>=1.0.3,<2

# Extensions
sphinxcontrib-bibtex>=2.6   # {cite}`key` + {bibliography} directive
sphinxext-opengraph>=0.9    # Open Graph / Twitter card metadata + social cards
sphinx-sitemap>=2.6         # sitemap.xml generation

# Scientific / notebook runtime
matplotlib>=3.8
numpy>=1.26
sympy>=1.13
ipykernel>=6.29
ipywidgets>=8.1
